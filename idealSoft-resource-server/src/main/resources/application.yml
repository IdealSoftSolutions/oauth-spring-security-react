server:
  port: 8080



logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/idealsoft_solutions
    username: postgres
    password: root
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-server:9000